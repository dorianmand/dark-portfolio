# Dark Portfolio Landing Page

React + Vite + Tailwind CSS + TypeScript + GSAP + Framer Motion + hls.js implementation of the requested single-page portfolio landing page.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```
