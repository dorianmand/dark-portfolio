/**
 * Removed 15 Aug 2026 — see docs/content-purge.md §2.5.
 *
 * A 2.7-second counter that blocked all content on first visit while cycling
 * the words "Better / workflows / with AI" — a consultancy tagline, and a gate
 * in front of an audience (hiring managers, practice principals) that will not
 * wait for it. The ?skipIntro=1 escape existed but nobody arrives with it.
 *
 * The file is emptied rather than deleted because the remote file bridge cannot
 * delete files; delete it manually.
 */
export {};
